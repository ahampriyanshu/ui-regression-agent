# TODO:  Write the prompt for Classification Agent
# Task: analyze differences against JIRA tickets and return JSON with resolved, pending, or new_tickets in the required format.
# The visual differences and JIRA tickets are passed along with the prompt; focus on writing instructions only.